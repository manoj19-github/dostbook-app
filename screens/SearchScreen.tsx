import {View, Text} from 'react-native';
import React, {FC} from 'react';

type SearchScreenProps = {};
const SearchScreen: FC<SearchScreenProps> = (): JSX.Element => {
  return (
    <View>
      <Text>SearchScreen</Text>
    </View>
  );
};

export default SearchScreen;
